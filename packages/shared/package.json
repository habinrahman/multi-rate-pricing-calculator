{
  "name": "@multi-rate/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests --pool=threads --poolOptions.threads.singleThread"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
